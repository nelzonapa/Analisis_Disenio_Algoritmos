/*
13. Disponemos de una memoria caché con capacidad para almacenar k datos.
Cuando la caché recibe una solicitud "di", puede que el dato ya esté en la caché
[hit] o que tengamos que buscarlo en memoria [miss], en cuyo caso
reemplazaremos alguno de los datos ya almacenados en la caché por el dato que
acabamos de obtener. Si conocemos de antemano el conjunto de solicitudes que
deberemos atender d1..dm, diseñe un algoritmo greedy que minimice el número
de fallos de caché.
*/

/*

*/